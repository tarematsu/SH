# Active optimization scope

Worker: `sh-minute-derive`

Goal: reduce per-Invocation CPU time toward 10 ms without changing queue durability, D1 semantics, retry behavior, ordering, or payload compatibility.

This temporary marker exists only to make the reservation visible before implementation and will be removed before review.
