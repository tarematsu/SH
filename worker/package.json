{
  "name": "stationhead-monitor-worker",
  "version": "1.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "postinstall": "node --test ../site/tests/data-maintenance-cadence.integration.test.js",
    "dev": "wrangler dev --test-scheduled",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "check": "npm run check:syntax && npm run check:bundle",
    "check:syntax": "node --check src/main.js && node --check src/cadenced-entry.js && node --check src/resilient-entry.js && node --check src/email-recap-index.js && node --check src/cloud-host-monitor.js && node --check src/cloud-weekly-leaderboard.js && node --check tests/collector.integration.test.js && node --check tests/diagnostic-cadence.test.js",
    "check:bundle": "wrangler deploy --dry-run --outdir=dist-dry-run",
    "test": "node --test tests/*.test.js"
  },
  "devDependencies": {
    "wrangler": "^4.104.0"
  }
}
