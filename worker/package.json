{
  "name": "stationhead-monitor-worker",
  "version": "1.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --test-scheduled",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "check": "npm run check:syntax && npm run check:bundle",
    "check:syntax": "node --check src/main.js && node --check src/email-recap-index.js && node --check src/cloud-host-monitor.js && node --check src/cloud-weekly-leaderboard.js",
    "check:bundle": "wrangler deploy --dry-run --outdir=dist-dry-run"
  },
  "devDependencies": {
    "wrangler": "^4.104.0"
  }
}
