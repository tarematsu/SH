// Canonical paginated minute-facts API. The implementation remains shared with
// the historical compatibility route until that route can be removed safely.
export { onRequestGet } from '../history-migrated.js';
