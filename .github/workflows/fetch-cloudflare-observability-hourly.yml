name: Check rolling-hour Worker CPU budget

on:
  pull_request:
    branches: [main]
    paths:
      - "worker/**"
      - ".github/workflows/fetch-cloudflare-observability-hourly.yml"
      - ".github/scripts/analyze-worker-observability.py"
      - ".github/scripts/enforce-worker-cpu-budget.py"
  schedule:
    - cron: "23 * * * *"
  workflow_dispatch:

permissions:
  actions: read
  contents: read

concurrency:
  group: cloudflare-observability-hourly-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  fetch:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_BUILDS_API_TOKEN }}
      AWS_DEFAULT_REGION: auto
      R2_BUCKET: cloudflare-managed-1609110a
      LOOKBACK_MINUTES: "60"
      LOG_SINCE: ""

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Wait for the current PR Worker deployment
        if: github.event_name == 'pull_request'
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          HEAD_BRANCH: ${{ github.event.pull_request.head.ref }}
          BASE_BRANCH: ${{ github.event.pull_request.base.ref }}
        run: |
          set -euo pipefail
          runtime_changed_at="$(git log --no-merges -1 --format=%cI "origin/${BASE_BRANCH}..HEAD" -- \
            worker/src worker/scripts 'worker/wrangler*.jsonc' \
            worker/package.json worker/package-lock.json)"
          if [[ -z "$runtime_changed_at" ]]; then
            echo "No runtime changes in this PR; using the rolling-hour lookback."
            exit 0
          fi
          runtime_changed_at="$(date -u -d "$runtime_changed_at" +%Y-%m-%dT%H:%M:%SZ)"
          deployed_at=""
          for attempt in $(seq 1 20); do
            runs="$(curl --fail-with-body --silent --show-error --get \
              -H "Authorization: Bearer $GH_TOKEN" \
              -H 'Accept: application/vnd.github+json' \
              "https://api.github.com/repos/${GITHUB_REPOSITORY}/actions/workflows/cloudflare-pr-diagnostics.yml/runs" \
              --data-urlencode "branch=$HEAD_BRANCH" \
              --data-urlencode 'event=pull_request' \
              --data-urlencode 'status=completed' \
              --data-urlencode 'per_page=100')"
            deployed_at="$(jq -r --arg changed "$runtime_changed_at" \
              '[.workflow_runs[] | select(.conclusion == "success" and .created_at >= $changed)] | max_by(.updated_at) | .updated_at // empty' \
              <<<"$runs")"
            if [[ -n "$deployed_at" ]]; then break; fi
            echo "Waiting for Cloudflare deployment attempt $attempt/20"
            sleep 15
          done
          test -n "$deployed_at" || { echo 'No successful PR deployment found after runtime changes' >&2; exit 1; }
          audit_since="$(date -u -d "$deployed_at + 90 seconds" +%Y-%m-%dT%H:%M:%SZ)"
          echo "Runtime changed at $runtime_changed_at"
          echo "Deployment completed at $deployed_at; auditing after propagation grace at $audit_since"
          echo "LOG_SINCE=$audit_since" >> "$GITHUB_ENV"

      - name: Derive R2 credentials
        shell: bash
        run: |
          set -euo pipefail
          test -n "$CLOUDFLARE_API_TOKEN"
          verify="$(curl --fail-with-body --silent --show-error -H "Authorization: Bearer $CLOUDFLARE_API_TOKEN" https://api.cloudflare.com/client/v4/user/tokens/verify)"
          token_id="$(jq -er '.result.id' <<<"$verify")"
          secret="$(printf '%s' "$CLOUDFLARE_API_TOKEN" | sha256sum | cut -d' ' -f1)"
          accounts="$(curl --fail-with-body --silent --show-error -H "Authorization: Bearer $CLOUDFLARE_API_TOKEN" 'https://api.cloudflare.com/client/v4/accounts?per_page=50')"
          export AWS_ACCESS_KEY_ID="$token_id" AWS_SECRET_ACCESS_KEY="$secret"
          match=""
          while read -r account_id; do
            endpoint="https://${account_id}.r2.cloudflarestorage.com"
            if aws s3api head-bucket --endpoint-url "$endpoint" --bucket "$R2_BUCKET" >/dev/null 2>&1; then
              test -z "$match" || { echo 'Multiple matching accounts' >&2; exit 1; }
              match="$account_id"
            fi
          done < <(jq -r '.result[].id' <<<"$accounts")
          test -n "$match" || { echo "R2 bucket not accessible: $R2_BUCKET" >&2; exit 1; }
          echo "::add-mask::$token_id"
          echo "::add-mask::$secret"
          echo "AWS_ACCESS_KEY_ID=$token_id" >> "$GITHUB_ENV"
          echo "AWS_SECRET_ACCESS_KEY=$secret" >> "$GITHUB_ENV"
          echo "R2_ENDPOINT=https://${match}.r2.cloudflarestorage.com" >> "$GITHUB_ENV"

      - name: Download rolling-hour Worker logs
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p raw observability-logs
          for attempt in $(seq 1 6); do
            rm -f objects.json keys.txt selection.json
            aws s3api list-objects-v2 --endpoint-url "$R2_ENDPOINT" --bucket "$R2_BUCKET" --output json > objects.json
            python3 - <<'PY'
          import datetime as dt, json, os, pathlib
          data = json.loads(pathlib.Path('objects.json').read_text(encoding='utf-8'))
          objects = data.get('Contents') or []
          since = os.environ.get('LOG_SINCE', '').strip()
          cutoff = dt.datetime.fromisoformat(since.replace('Z', '+00:00')) if since else (
              dt.datetime.now(dt.timezone.utc) - dt.timedelta(minutes=int(os.environ['LOOKBACK_MINUTES']))
          )
          if cutoff.tzinfo is None:
              cutoff = cutoff.replace(tzinfo=dt.timezone.utc)
          selected = []
          for item in objects:
              modified = dt.datetime.fromisoformat(str(item['LastModified']).replace('Z', '+00:00'))
              if modified >= cutoff:
                  selected.append((modified, item['Key']))
          selected.sort(reverse=True)
          selection = {
              'cutoff': cutoff.isoformat().replace('+00:00', 'Z'),
              'objects_available': len(objects),
              'objects_selected': len(selected),
              'newest_object_modified': selected[0][0].isoformat().replace('+00:00', 'Z') if selected else None,
              'oldest_object_modified': selected[-1][0].isoformat().replace('+00:00', 'Z') if selected else None,
          }
          pathlib.Path('selection.json').write_text(json.dumps(selection, ensure_ascii=False, indent=2) + '\n', encoding='utf-8')
          pathlib.Path('keys.txt').write_text(''.join(f'{key}\n' for _, key in selected), encoding='utf-8')
          print(f"Selected {len(selected)} objects modified since {cutoff.isoformat()}")
          PY
            if [[ -s keys.txt ]]; then break; fi
            echo "Waiting for R2 log delivery attempt $attempt/6"
            sleep 30
          done
          test -s keys.txt || { echo 'No post-deployment observability objects found' >&2; exit 1; }
          index=0
          pids=()
          while IFS= read -r key; do
            index=$((index + 1))
            file="raw/$(printf '%04d' "$index").log"
            aws s3api get-object --endpoint-url "$R2_ENDPOINT" --bucket "$R2_BUCKET" --key "$key" "$file" >/dev/null &
            pids+=("$!")
            if (( ${#pids[@]} >= 8 )); then
              for pid in "${pids[@]}"; do wait "$pid"; done
              pids=()
            fi
          done < keys.txt
          for pid in "${pids[@]}"; do wait "$pid"; done

      - name: Audit rolling-hour Workers and CPU time
        shell: bash
        run: python3 .github/scripts/analyze-worker-observability.py

      - name: Enforce stable 8 ms Worker CPU budget
        if: always()
        shell: bash
        run: python3 .github/scripts/enforce-worker-cpu-budget.py

      - name: Publish rolling-hour summary
        if: always()
        shell: bash
        run: |
          if [[ ! -f observability-logs/summary.json ]]; then exit 0; fi
          python3 - <<'PY' >> "$GITHUB_STEP_SUMMARY"
          import json, pathlib
          summary = json.loads(pathlib.Path('observability-logs/summary.json').read_text(encoding='utf-8'))
          print(f"## Rolling-hour Worker audit: {'OK' if summary['ok'] else 'FAILED'}")
          print()
          print(f"- Window start: `{summary['since']}`")
          print(f"- Worker events: `{summary['events']}`")
          print(f"- Error events: `{summary['error_events']}`")
          print(f"- CPU ms: avg `{summary['cpu_ms']['avg']}`, p95 `{summary['cpu_ms']['p95']}`, max `{summary['cpu_ms']['max']}`")
          print()
          print('| Worker | events | CPU avg ms | CPU p95 ms | CPU max ms |')
          print('|---|---:|---:|---:|---:|')
          for name, item in summary['scripts'].items():
              metric = item['cpu_ms']
              print(f"| {name} | {item['events']} | {metric['avg']} | {metric['p95']} | {metric['max']} |")
          PY

      - name: Upload rolling-hour Worker audit
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cloudflare-observability-hourly-${{ github.run_id }}
          path: observability-logs/
          if-no-files-found: warn
          retention-days: 7
