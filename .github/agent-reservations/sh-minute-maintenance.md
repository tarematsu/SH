# Agent reservation: sh-minute-maintenance

This branch reserves `sh-minute-maintenance` for per-Invocation CPU optimization toward the 10 ms target.

Implementation has not started. The draft pull request is being opened first so concurrent agents can avoid this Worker and its dedicated runtime/test files.
