# CPU optimization scope claim

Selected Worker: `sh-buddies-ingest`.

This temporary marker reserves the `sh-buddies-ingest` Worker for focused per-Invocation CPU optimization toward the 10 ms ceiling.

Initial implementation scope is limited to the `sh-buddies-ingest` runtime entry/hot path and dedicated regression tests. No other Worker is in scope. The marker will be removed before final review.
